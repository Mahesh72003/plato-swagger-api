asyncapi: '2.0.0'
id: 'urn:com:oracle:fsgbu:obcm:asyncapi:limitsStructureUpdated'
info:
  title: Kafka Application - limits structure updated event
  version: '1.0.0'
  description: Kafka Application - limits structure updated event
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
  development:
    url: whf00mip.in.oracle.com:9092
    description: Development server
    protocol: kafka
channels:
  limitsStructureUpdated:
    description: limits structure updated topic
    subscribe:
      summary: Get limits structure updated event
      message:
        description: Get updated structure limits list
        $ref: '#/components/messages/ReportsLimitsList'          

components:
  messages:
    ReportsLimitsList:
      name: Update Limits Structure Message
      title: Update Limits Structure
      summary: Update Limits Structure Message
      contentType: application/json
      payload:
        $ref: "#/components/schemas/ReportsLimitsList"
  schemas:
    ReportsLimitsList:
      type: object     
      properties:
        resourceId: 
          type: string
        userId:
          type: string
        branchCode:
          type: string