asyncapi: '2.0.0'
id: 'urn:com:oracle:fsgbu:obcm:asyncapi:createExternalDelinqStatus'
info:
  title: Kafka Application - createExternalDelinqStatus event
  version: '1.0.0'
  description: Kafka Application - createExternalDelinqStatus event
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
  development:
    url: whf00mip.in.oracle.com:9092
    description: Development server
    protocol: kafka
channels:
  createExternalDelinqStatus:
    description: createExternalDelinqStatus topic
    subscribe:
      summary: Get createExternalDelinqStatus event
      message:
        description: Get createExternalDelinqStatus list
        $ref: '#/components/messages/CreateExternalDelinqDto'

components:
  messages:
    CreateExternalDelinqDto:
      name: Update createExternalDelinqStatus Message
      title: Update createExternalDelinqStatus
      summary: Update createExternalDelinqStatus Message
      contentType: application/json
      payload:
        $ref: "#/components/schemas/CreateExternalDelinqDto"
  schemas:
    CreateExternalDelinqDto:
      type: object
      properties:
        extCustomerDelinqStatusId:
          type: string
        userId:
          type: string
        serviceBranchCode:
          type: string
        createExtCustomerDelinqStatusHisDto:
          type: array
          description: CreateExtCustomerDelinqStatusHisDto
          items:
            $ref: "#/components/schemas/CreateExtCustomerDelinqStatusHisDto"
    CreateExtCustomerDelinqStatusHisDto:
      type: object
      properties:
        id:
          type: string
        customerId:
          type: string
        delinquencyStatus:
          type: string
        activeStatus:
          type: string
        statusDate:
          type: string
        inputDate:
          type: string
        channel:
          type: string
        event:
          type: string
        customerName:
          type: string
        deliquencyDescription:
          type: string
        priority:
          type: string