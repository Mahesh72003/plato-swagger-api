asyncapi: '2.0.0'
id: 'urn:com:oracle:fsgbu:obcm:asyncapi:limitsAmountUpdated'
info:
  title: Kafka Application - limits amount updated event
  version: '1.0.0'
  description: Kafka Application - limits amount updated event
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0
servers:
  development:
    url: whf00mip.in.oracle.com:9092
    description: Development server
    protocol: kafka
channels:
  limitsAmountUpdated:
    description: limits amount updated topic
    subscribe:
      summary:  limits amount updated event
      message:
        description: update limits amount
        $ref: '#/components/messages/LimitLineIdList'          

components:
  messages:
    LimitLineIdList:
      name: Update Limits amount Message
      title: Update Limits amount
      summary: Update Limits amount Message
      contentType: application/json
      payload:
        $ref: "#/components/schemas/LimitLineIdList"
  schemas:
    LimitLineIdList:
      type: object     
      properties:
        lineIds: 
          type: array
          items:
             type: string
        userId:
          type: string
        serviceBranchCode:
          type: string