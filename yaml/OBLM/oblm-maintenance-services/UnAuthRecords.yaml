# /*******************************************************************************
# * This source is part of the Oracle Banking Software Product.
# * Copyright (c) 2018,2021, Oracle and/or its affiliates. All rights reserved.
# *
# * No part of this work may be reproduced, stored in a retrieval system, adopted or tr
# *
# * Oracle Financial Services Software Limited.
# * Oracle Park, Off Western Express Highway,
# * Goregaon (East),
# * Mumbai - 400 063, India.
# ******************************************************************************/
swagger: '2.0'
info:
  description: This is a sample server sweepproducts server.
  version: 1.0.0
  title: UnAuthorized Records
host: api-plato.us.oracle.com:8080
basePath: /api/1.0
consumes:
- application/json
produces:
- application/json
tags:
- name: unAuthorizedRecords
  description: ''
schemes:
- https
paths:
  /unAuthorizedRecords:
    get:
      tags:
      - unAuthorizedRecords
      summary: Get all valid UnAuthorized Records.
      description: Get all valid UnAuthorized Records. This endpoints will be used to Get all valid UnAuthorized maintenance records.
      operationId: getAll
      parameters:
      - in: query
        name: branchCode
        required: false
        type: string     
      responses:
        201:
          description: Returns message of succes or failure
          headers:
            Location:
              description: ""
              type: string
          schema:
            $ref: '#/definitions/LmmTmUnAuthorizedRecordCollection'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error      
definitions:
  LmmTmUnAuthRecordsDTO:
    type: object
    allOf:
    - $ref: '#/definitions/GenericDto'
    - type: object
      properties:
        maintenance:
          type: string
          description: ""
        data:
          type: string
          description: ""                  
  ModListKeyDto:
    type: object
    properties:
      modNos:
        type: array
        items:
          type: integer
      remarks:
        type: string
  KeyDto:
    type: object
    properties:
      modNo:
        type: integer
      remarks:
        type: string
  ResponseDto:
    type: object
    properties:
      id:
        type: string
      status:
        type: string
      codes:
        type: array
        items:
          $ref: '#/definitions/ResponseCode'
      requestId:
        type: string
  ResponseCode:
    type: object
    properties:
      Code:
        type: string
      Desc:
        type: string
      Type:
        type: string
      Language:
        type: string
      args:
        type: array
        items:
          type: object
      arg:
        type: string
  GenericDto:
    allOf:
    - type: object
      discriminator: resource_type
  LmmTmUnAuthorizedRecordCollection:
    description: Collection of records.
    type: object
    properties:
      data:
        type: array
        items:
          $ref: '#/definitions/LmmTmUnAuthRecordsDTO'
      paging:
        $ref: '#/definitions/PagingDto'
  PagingDto:
    type: object
    properties:
      totalResults:
        type: integer
  ResponseDtoWrapper:
    type: object
    properties:
      messages:
        $ref: '#/definitions/ResponseDto'
      data:
        $ref: '#/definitions/ResponseResourceSupport'
  ResponseResourceSupport:
    type: object
  LmmTmUnAuthWrapperDTO:
    type: object
    properties:
      data:
        $ref: '#/definitions/LmmTmUnAuthRecordsDTO'