swagger: '2.0'
info:
  description: You can find out more about Swagger at [swagger.io](http://swagger.io) and about data types at [Swagger Data Types](https://swagger.io/docs/specification/data-models/data-types/)
  version: 1.0.0
  title: Decisioningmaster Services
host: 10.184.157.121:9000
basePath: /obcm-collections-maintenance-services
tags:
- name: DecisioningmasterAggregateWeb
  description: API for data segment crud operations for UI
- name: DecisioningmasterAggregateService
  description: API for data segment crud operations for Services
schemes:
- http
paths:
  /web/v1/decisioningmaster:
    post:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Adds a new datasegment details.
      description: Adds a new datasegment details.
      operationId: createResource
      parameters:
      - name: decisioningmasterWebModel
        in: body
        description: Request Body that contains data required for creating a new datasegment
        required: true
        schema:
         $ref: '#/definitions/DecisioningmasterWebModel'
      responses:
        201:
          description: Returns the Id of the newly created datasegment
          headers:
            Location:
              description: URL of newly created datasegment
              type: string
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'              
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
    get:
     tags:
      - DecisioningmasterAggregateWeb
     summary: "Get all valid master datasegment details"
     description:  "Get all valid master datasegment. This endpoints will be used to Get all valid master datasegment maintenance records."
     operationId: "getSummary"
     parameters:
     - name: includecloseandunauth
       in: query
       description: includecloseandunauth need to be set to fetch summary record, defult value is false
       required: false
       type: boolean
     - name: offset
       in: query
       description: offset is starting no of records to be fetched
       required: false
       type: integer
     - name: limit
       in: query
       description: limit is maximum no of records to be fetched
       required: false
       type: integer
     - name: product
       in: query
       description: product need to be set to fetch summary record
       required: false
       type: string
     - name: effectiveDate
       in: query
       description: effectiveDate need to be set to fetch summary record
       required: false
       type: string
     - name: arrangementParty
       in: query
       description: arrangementParty need to be set to fetch summary record
       required: false
       type: string
     - name: filterCriteria
       in: query
       description: filterCriteria need to be set to fetch summary record
       required: false
       type: string        
     - name: authStatus
       in: query
       description: auth status of the records to be fetched
       required: false
       type: string
     - name: recordStatus
       in: query
       description: records status of the records to be fetched
       required: false
       type: string
     - name: corporateName
       in: query
       description: corporate name to be fetched
       required: false
       type: string
     - name: correspondentBank
       in: query
       description: correspondent bank name to be fetched
       required: false
       type: string
     responses:
      200:
       description: "Returns message of succes or failure"
       headers: 
        Location:
         description: ""
         type: "string"
       schema:
         $ref: '#/definitions/ObcmTmDecisioningMasterCollection'
      400:
       description: "Malformed syntax."
      401:
       description: "Request does not have sufficient credentials."
      403:
       description: "Request is not authorized."
      404:
       description: "Not found"       
      405:
       description: "Invalid input"
      500:
       description: "Internal error"            
  /web/v1/decisioningmaster/{resourceId}:
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Retrieves the datasegment deatils by resourceId.
      description: Retrieves the datasegment deatils by resourceId.
      operationId: getByResourceId
      parameters:
      - name: resourceId
        in: path
        description: Resource Id of the datasegment that needs to be fetched
        required: true
        type: string
      - name: datasegmentCode
        in: query
        description: datasegmentCode of the datasegment
        type: string
      - name: version
        in: query
        description: version of the datasegment
        type: string
      - name: latestAuthRecord
        in: query
        description: "provides only latest lates auth records if true"
        required: false
        default: false
        type: boolean               
      responses:
        200:
          description: Returns the datasegment based on given resourceId.
          schema:
            $ref: '#/definitions/DecisioningmasterWebModel'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        404:
          description: Not found       
        500:
          description: Internal error
    put:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Update an existing datasegment.
      description: Update an existing datasegment.
      operationId: updateResource
      parameters:
      - name: resourceId
        in: path
        description: Resource Id of the resource that need to be updated
        required: true
        type: string
      - name: decisioningmasterWebModel
        in: body
        description: Request Body that contains data required for updating an existing datasegment
        required: true
        schema:
          $ref: '#/definitions/DecisioningmasterWebModel'
      responses:
        202:
          description: Returns updated datasegment Details
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'          
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error  
  /web/v1/decisioningmaster/{resourceId}/delete:
    put:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Delete the resource based on the resourceId and version.
      description: Deleting the resource.
      operationId: deleteResource
      parameters:
      - name: resourceId
        in: path
        description: Resource Id of the resource that need to be deleted
        required: true
        type: string
      - name: orchestratorDTO
        in: body
        description: Request body contains resource Id and resource mod numbers which need to be deleted
        required: true
        schema:
          $ref: '#/definitions/OrchestratorDTO'
      responses:
        200:
          description: "Successful operation."
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
  /web/v1/decisioningmaster/{resourceId}/isValid:
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Validates the resource by Resource Id.
      description: Validates the resource by Resource Id.
      operationId: validateResource
      parameters:
      - name: resourceId
        in: path
        description: Resource Id that needs to be validated
        required: true
        type: string
      - name: version
        in: query
        description: version for which validation is required
        required: true
        type: integer        
      responses:
        200:
          description: Successful operation.
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error

  /web/v1/decisioningmaster/{id}/reject:
    patch:
      tags:
      - DecisioningmasterAggregateWeb
      summary: "Rejects the resource by given Resource Id"
      description:  "Rejects the resource by given Resource Id"
      operationId: "reject"
      parameters:
      - name: id
        in: path
        description: Resource Id that needs to be closed
        required: true
        type: string     
      - name: orchestratorDTO
        in: body
        description: Request body contains resource Id and resource mod numbers which need to be rejected
        required: true
        schema:
          $ref: '#/definitions/OrchestratorDTO'    
      responses:
        200:
          description: Successful operation.
          schema:
            $ref: '#/definitions/ResponseDtoWrapper' 
        400:         
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error          
          
  /web/v1/decisioningmaster/{resourceId}/submit:
    patch:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Submit for the Resource.
      description: Validates datasegments and submits the resource.
      operationId: submitResource
      parameters:
      - name: resourceId
        in: path
        description: Resource Id that needs to be submited
        required: true
        type: string
      - name: orchestratorDTO
        in: body
        description: Request body contains resource Id and resource mod numbers
        required: true
        schema:
          $ref: '#/definitions/OrchestratorDTO'        
      responses:
        200:
          description: Returns the success
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
  /web/v1/decisioningmaster/{resourceId}/unauthorized:
    get:
      tags:
      - DecisioningmasterAggregateWeb
      summary: "Retrives the unauthorized resource by given resource id"
      description:  "Retrives the unauthorized resource identified by given resource id"
      operationId: "getUnAuthResource"
      parameters:
      - name: resourceId
        in: path
        description: Resource Id that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Returns the success
          schema:
            $ref: '#/definitions/ObcmTmDecisioningMasterCollection'
        400:           
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
  /web/v1/decisioningmaster/{resourceId}/authorize:
    put:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Authorize the resource.
      description: Authorize the resource and moves the latest data from all worktables to main tables.
      operationId: authorizeResource
      parameters:
      - name: resourceId
        in: path
        description: Resource id of the resource
        required: true
        type: string
      - name: orchestratorDTO
        in: body
        description: Request body contains resource Id and resource mod numbers which need to be authorized
        required: true
        schema:
          $ref: '#/definitions/OrchestratorDTO'
      responses:
        200:
          description: Returns the success
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
  /web/v1/decisioningmaster/{resourceId}/unlock:
    put:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: removes the advisory lock on the resource.
      description: removes the advisory lock on the resource.
      operationId: removeResourceLock
      parameters:
      - name: resourceId
        in: path
        description: resourceId of the resource
        required: true
        type: string
      responses:
        200:
          description: Successful operation.
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error    
  /web/v1/decisioningmaster/{resourceId}/actions:
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: provides action for Resource Id.
      description: provides action for Resource Id.
      operationId: getPermittedActionsOnRes
      parameters:
      - name: resourceId
        in: path
        description: Resource Id for which actions needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Successful operation.
          schema:
            $ref: '#/definitions/String'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
  /web/v1/decisioningmaster/{resourceId}/history:
    get:
      tags:
      - DecisioningmasterAggregateWeb  
      summary: "Retrives the history by given Resource Id"
      description:  "Retrives the history by given Resource Id"
      operationId: "getResourceHistory"
      parameters:
      - name: resourceId
        in: path
        description: resourceId of the resource
        required: true
        type: string      
      responses:
        200:
          description: Successful operation.
          schema:          
            $ref: '#/definitions/ObcmTmDecisioningMasterCollection'
 
        400:          
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error        
  /web/v1/decisioningmaster/{resourceId}/close:
    patch:
      tags:
      - DecisioningmasterAggregateWeb  
      summary: "Closes the resource by given Resource Id"
      description:  "Closes the resource by given Resource Id"
      operationId: "closeResource"
      parameters:
      - name: resourceId
        in: path
        description: Resource Id that needs to be closed
        required: true
        type: string      
      - name: orchestratorDTO
        in: body
        description: Request body contains resource Id and resource mod numbers which need to be closed
        required: true
        schema:
          $ref: '#/definitions/OrchestratorDTO'     
      responses:
        200:
          description: Successful operation.
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'  
        400:          
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error            
  /web/v1/decisioningmaster/{resourceId}/reopen:
    patch:
      tags:
      - DecisioningmasterAggregateWeb    
      summary: "reopen the resource by given Resource Id"
      description:  "reopen the resource by given Resource Id"
      operationId: "reopenResource"
      parameters:
      - name: resourceId
        in: path
        description: Resource Id that needs to be reopned 
        required: true
        type: string      
      - name: orchestratorDTO
        in: body
        description: Request body contains resource Id and resource mod numbers which need to be reopened
        required: true
        schema:
         $ref: "#/definitions/OrchestratorDTO"        
      responses:
        200:
          description: Successful operation.
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'   
        400:         
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error 
  /web/v1/decisioningmaster/{keyId}/aggregate:
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - DecisioningmasterAggregateWeb
      summary: Get aggregate details of the resource
      description: Get aggregate details of the resource
      operationId: getResourceAggregate
      parameters:
      - name: keyId
        in: path
        description: keyId for which the data needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Returns Aggregate data for the resource identified by keyid
          schema:
            $ref: '#/definitions/DecisioningmasterWebModel'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
          
  /web/v1/arrangementDecisioning/arrangementDays:
   post:
    tags:
     - "DecisioningmasterAggregateWeb"
    summary: "Retrieves the Arrangement days by given parameters."
    description:  "Retrieves the Arrangement days by given parameters.."
    operationId: "getArrangementDaysWeb"
    parameters:
     - name: "userId"
       in: "header"
       description: "userId"
       required: true
       type: "string"
     - name: "branchCode"
       in: "header"
       description: "branchCode"
       required: true
       type: "string"
     - name: "appId"
       in: "header"
       type: "string"
       required: true
       description: "Application Id"
     - name: "decisioningDays"
       in: "body"
       description: "Request Body that contains data required for calculating Arrangement days"
       schema:
        $ref: "#/definitions/DecisioningDaysDTO"
    responses:
     200:
      description: "Returns the Arrangement Days based on given parameters."
      schema:
        $ref: "#/definitions/DecisioningDaysResponseDTO"
     400:
      description: "Malformed syntax."
     401:
      description: "Request does not have sufficient credentials."
     403:
      description: "Request is not authorized."
     500:
      description: "Internal error"          
                                     
definitions:
  DecisioningmasterAggregateModel:
    description: DecisioningmasterAggregateModel
    type: object
    allOf:
    - $ref: '#/definitions/BaseGenericModelDS'
    - type: object
      properties:    
       keyId:
        description: key Id
        type: string
       obcmTmDecisioningMaster:
        $ref: '#/definitions/ObcmTmDecisioningMasterDTO'
       extendedDatasegments:
        description: data for the extended datasegment operation
        type: array
        items:
          type: object
  BaseGenericModelDS:
   allOf:
    - type: object
      discriminator: resource_type                         
  DecisioningmasterAggregateModelCollection:
    description: Collection of  DecisioningmasterAggregateModel.
    type: object
    properties:
      data:
        type: array
        items:
          $ref: '#/definitions/DecisioningmasterAggregateModel'
  DecisioningmasterWebModel:
    description: DecisioningmasterWebModel
    type: object
    properties:
      resourceId:
        description: resource id
        type: string
      resourceName:
        description: resource name
        type: string
      operationType:
        description: operation type
        type: string
      makerId:
        description: maker id
        type: string
      makerDateStamp:
        description: make date stamp
        type: string
      checkerId:
        description: check id
        type: string
      checkerDateStamp:
        description: checker date stamp
        type: string
      modNo:
        description: mod no
        type: integer
      recordStatus:
        description: record status flag
        type: string
      authStatus:
        description: auth status flag
        type: string
      onceAuth:
        description: once auth flag
        type: string
      doerRemarks:
        description: doer remarks
        type: string
      approverRemarks:
        description: approver  remarks
        type: string        
      obcmTmDecisioningMaster:
        $ref: '#/definitions/ObcmTmDecisioningMasterDTO'
      extendedDatasegments:
        description: data for the extended datasegment operation
        type: array
        items:
          type: object
    required:
        - resourceId
        - operationType         
  DecisioningmasterWebModelCollection:
    description: Collection of datasegment.
    type: object
    properties:
      data:
        type: array
        items:
          $ref: '#/definitions/DecisioningmasterWebModel'


  DecisioningDaysDTO:
    description: "Decisioning Days Request model"
    type: "object"
    properties:
      corporateId:
        type: string
        description: "Corporate Id for which Arrangement days are required"
      correspondentBankId:
        type: string
        description: "Correspondent Bank Id for which Arrangement days are required"
      draweebankId:
        type: string
        description: "Drawee Bank Id for which Arrangement days are required"
      locationId:
        type: string
        description: "Location Id for which Arrangement days are required"
      arrangementParty:
        type: string
        description: "Arrangement Party for which Arrangement days are required"
      amount:
        type: number
        description: "Amount"
      ccy:
        type: string
        description: "Currency"
      productCode:
        type: string
        description: "Product Code"                  
  DecisioningDaysResponseDTO:
    description: "Decisioning Days Response model"
    type: "object"
    properties:
      arrangementDays:
        description: "Required Arrangement Days"
        type: number
      certificateExtensionDays:
        description: "Required certificateExtensionDays"
        type: number
      corporateReleaseDays:
        description: "Required corporateReleaseDays"
        type: number
      lostExtensionDays:
        description: "Required lostExtensionDays"
        type: number
      returnExtensionDays:
        description: "Required returnExtensionDays"
        type: number
      arrangementCode:
        description: "Required arrangementCode"
        type: string
      arrangementDesc:
        description: "Required arrangementDesc"
        type: string
      arrangementRefBasis:
        description: "Required arrangementRefBasis"
        type: string
      cdtReversalDays:
        description: "Credit Reversal Days"
        type: number
      cdtReversalFrom:
        description: "Credit Rerversal From"
        type: string
      partialCdtReversal:
        description: "Partial Credit Reversal"
        type: string
      bankFltAgmt:
        description: "Bank Float Arrangement Flag"
        type: string
      rtnRecoveryFrom:
        description: "Return Recovery From"
        type: string
      partialRtnRecovery:
        description: "Partial Return Recovery"
        type: string
      fcdDbtFrRecovery:
        description: "Forced Debit For Recovery"
        type: string 
      centralizedSchedule:
        description: "Centralized Forward Schedule"
        type: string
      inclusionOfHolidays:
        description: "Inclusion Of Holidays"
        type: string
      agreementRefNo:
        description: "agreementRefNo"
        type: string   
  FilterOutput:
    description: "Represents the best possible combination for the given category"
    type: "object"
    properties:
      attributeList:
        type: "array"
        description: "List of attributes."
        items:
          $ref: "#/definitions/Attribute"
  Attribute:
    description: "Represents each attribute of the records filtered in calling service using filter query"
    type: "object"
    properties:
      attributeName:
        description: "Name of the attribute representing DB column name"
        type: "string"
      attributeValue:
        description: "Value of the corresponding column in DB"
        type: "string"
  FilterRecords:
    description: "Represents the records filtered in calling service using filter query"
    type: "object"
    properties:
      attributeList:
        type: "array"
        description: "List of attributes."
        items:
          $ref: "#/definitions/Attribute"
  FilterInput:
    description: "Represents the input request of the final filter service call from transaction services"
    type: "object"
    properties:
      category:
        description: "Represents the category of the filter"
        type: "string"
      applicationCode:
        description: "Represents the applicationcode"
        type: "string"
      filterRecords:
        type: "array"
        description: "List of Filter records"
        items:
          $ref: "#/definitions/FilterRecords"
  ScfTransOutputResponse:
    description: "Output response from transaction API"
    type: "object"
    properties:
      queryList:
        type: "array"
        description: "List of queries"
        items:
          $ref: "#/definitions/FilterQueryModel"
  FilterQueryModel:
    description: "Query and attribute list model"
    type: "object"
    properties:
      query:
        description: "Filter Query"
        type: "string"
      ScfTransCriteriaQueryAttributeModel:
        type: "array"
        description: "SCF Transfer Criteria Query Attribute Model Object"
        items:
          $ref: "#/definitions/TransCriteriaQueryAttributeModel"
  TransCriteriaQueryAttributeModel:
    description: "Query for each type of given category"
    type: "object"
    properties:
      paramIndex:
        description: "input parameter index in the query"
        type: "integer"
      paramName:
        description: "input parameter name in the query"
        type: "string"
      columnName:
        description: "corresponding column of the input parameter index in the query"
        type: "string"
      mappedParam:
        description: "corresponding mapped param index in the query"
        type: "string"        
  ObcmTmDecisioningMasterCollection: 
   type: "object"  
   description: "Collection of decisioning Master Objects."
   properties:
    data:
     type: "array"
     description: "Data."
     items: 
      $ref: "#/definitions/ObcmTmDecisioningMasterDTO"
    paging:
     $ref: "#/definitions/PagingDto"
  PagingDto:
   type: object
   description: "Paging Object"
   properties:
    totalResults:
     type: integer
     description: "No of Total Results."
  ObcmTmDecisioningMasterDTO:
   type: object
   description: "OBCM TM Decisioning Master DTO"
   allOf:
   - $ref: '#/definitions/GenericDto'
   - type: object
     properties:
      agreementRefNumber:
       type: string
       description: "Agreement Reference Number."
      amountBasedArrangement:
       type: string
       description: "Amount Based Arrangement."
      arrangementParty:
       type: string
       description: "Arrangement Party."
      arrangementPartyName:
       type: string
       description: "Arrangement Party Name."
      centralizedForwardSchedule:
       type: string
       description: "Centralized Forward Schedule."
      corporate:
       type: string
       description: "Corporate Name."
      corporateId:
       type: string
       description: "Corporate Id."
      correspondentBank:
       type: string
       description: "Correspondant Bank Name."
      correspondentBankId:
       type: string
       description: "Correspondant Bank Id."
      draweebankId:
       type: string
       description: "Drawee Bank Id."
      draweeBank:
       type: string
       description: "Drawee Bank."
      effectiveDate:
       type: string
       description: "Effective Date."
      expiryDate:
       type: string
       description: "Expiry Date."
      filterCriteria:
       type: string
       description: "Filter Criteria."
      inclusionOfHolidays:
       type: string
       description: "Inclusion Of Holidays."
      locationId:
       type: string
       description: "Location Id."
      locationName:
       type: string
       description: "Location Name."
      product:
       type: string
       description: "Product Name."
      productId:
       type: string
       description: "Product Id."
      ObcmTmDecisioningDetailsDTO:
       type: array
       description: "Obcm Tm Decisioning Details."
       items:
        $ref: '#/definitions/ObcmTmDecisioningDetailsDTO'
  ObcmTmDecisioningDetailsDTO:
   type: object
   description: "OBCM TM Decisioning Details."
   properties:
      arrangementCode:
       type: string
       description: "Arrangement Code."
      currency:
       type: string
       description: "Currency."
      arrangementDays:
       type: number
       description: "Arrangement Days."
      arrangementRefBasis:
       type: string
       description: "Arrangement Reference Basis."
      certificateExtensionDays:
       type: number
       description: "Certification Extension Days."
      corporateReleaseDays:
       type: number
       description: "Corporate Release Days."
      decisioningMasterId:
       type: string
       description: "Decisioning Master Id."
      fromAmount:
       type: number
       description: "From Amount."
      toAmount:
       type: number
       description: "To Amount."
      id:
       type: string
       description: "Id"
      linkArrangement:
       type: string
       description: "Arrangement Linked (Y/N)."
      lostExtensionDays:
       type: number
       description: "Lost Extension Days."
      returnExtensionDays:
       type: number
       description: "Return Extension Days."
      creditReversalDays:
       type: number
       description: "Credit Reversal Days."
      creditReversalFrom:
       type: string
       description: "Credit Reversal From."
      partialCreditReversal:
       type: string
       description: "Partial Credit Reversal."
      bankFloatArrangement:
       type: string
       description: "Bank Float Arrangement."
      returnRecoveryFrom:
       type: string
       description: "Return Recovery From."
      partialReturnRecovery:
       type: string
       description: "Partial Return Recovery."
      forcedDebitForRecovery:
       type: string
       description: "Forced Debit For Recovery."
  GenericDto:
   allOf:
    - type: object
    - description: "Generic Response."
      discriminator: resource_type
  ObcmTmDecisioningMasterWrapperDTO:
    type: object
    description: "Decisioning Master Wrapper DTO Object"
    properties:
     data:
      $ref: "#/definitions/ObcmTmDecisioningMasterDTO"          
  
      