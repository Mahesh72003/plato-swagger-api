#----------------------------- OFSS-COPYRIGHT-BEGIN ----------------------------- 
#
#  This source is part of the Oracle Banking Corporate Lending Process Management.
#  Copyright  2018 , Oracle and/or its affiliates.  All rights reserved.
#  
#  No part of this work may be reproduced, stored in a retrieval system, adopted or transmitted in any form or by any means, electronic, mechanical, photographic, graphic, optic recording or otherwise, translated in any language or computer language, without the prior written permission of Oracle and/or its affiliates.
#  
#  Oracle Financial Services Software Limited.
#  Oracle Park, Off Western Express Highway,
#  Goregaon (East), 
#  Mumbai - 400 063, India. 
#  
# ------------------------------- OFSS-COPYRIGHT-END -----------------------------
#
#-----------------------------------------------------------------------------------------------------
#  **
#  ** File Name  : trancheparticipants.yaml
#  **
#  ** Module     : 
#  ** 

#  -------------------------------------------------------------------------------------------------------
#
#  CHANGE HISTORY
#
#  Created By         :  System
#
#  Bug Number         :  
#  Changed By         :  
#  Change Description :  
#  Change On          :  
#  Search String      :  
#  
#  -------------------------------------------------------------------------------------------------------
swagger: '2.0'
info:
  description: You can find out more about Swagger at [swagger.io](http://swagger.io) and about data types at [Swagger Data Types](https://swagger.io/docs/specification/data-models/data-types/)
  version: 1.0.0
  title: Syndication Application Service
host: 10.184.157.121:9000
basePath: /syndicationapplication-services
tags:
- name: SyndicationApplication
  description: API for SyndicationApplication Operations
schemes:
- http
paths:
  /web/v1/trancheparticipants:
    post:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - TrancheParticipant
      summary: Adds a new TrancheParticipant.
      description: Adds a new TrancheParticipant. Add additional information here for this endpoint.
      operationId: addTrancheParticipant
      parameters:
      - name: TrancheParticipant
        in: body
        description: Request Body that contains data required for creating a new TrancheParticipant
        required: true
        schema:
          $ref: '#/definitions/TrancheParticipantModel'
      responses:
        201:
          description: Returns the Id of the newly created TrancheParticipant
          headers:
            Location:
              description: URL of newly created TrancheParticipant
              type: string
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - TrancheParticipant
      summary: Retrieves the list of existing TrancheParticipant.
      description: Retrieves the list of existing TrancheParticipant. Add additional information here for the this endpoint.
      operationId: getAllTrancheParticipant
      parameters:
      - name: applicationNumber
        in: query
        description: applicationNumber of the user that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Successful performs the operation
          schema:
            $ref: '#/definitions/ResponseModel'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
  /web/v1/trancheparticipants/{id}:
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - TrancheParticipant
      summary: Retrieves the TrancheParticipant by id.
      description: Retrieves the TrancheParticipant by id. Add additional information here for the this endpoint.
      operationId: getTrancheParticipantById
      parameters:
      - name: id
        in: path
        description: Id that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Returns the TrancheParticipant based on given applicationNumber.
          schema:
            $ref: '#/definitions/TrancheParticipantModel'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
    put:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - TrancheParticipant
      summary: Update an existing TrancheParticipant.
      description: Update an existing TrancheParticipant. Add additional information here for the this endpoint.
      operationId: updateTrancheParticipant
      parameters:
      - name: id
        in: path
        description: id of the TrancheParticipant that need to be updated
        required: true
        type: string
      - name: TrancheParticipant
        in: body
        description: Request Body that contains data required for updating an existing TrancheParticipant
        required: true
        schema:
          $ref: '#/definitions/TrancheParticipantModel'
      responses:
        200:
          description: Returns updated TrancheParticipant
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
    delete:
      produces:
      - application/json
      - application/xml
      tags:
      - TrancheParticipant
      summary: Deletes the TrancheParticipant identified by given ID.
      description: Deletes the TrancheParticipant identified by given ID. Add additional information here for the this endpoint.
      operationId: deleteTrancheParticipant
      parameters:
      - name: id
        in: path
        description: Id of the TrancheParticipant that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Successful operation.
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
definitions:
  TrancheParticipantModel:
    description: TrancheParticipant model
    type: object
    properties:
      id:
        description: Id of the TrancheParticipant
        type: string
        maxLength: 36
      dataSegmentCode:
        description: Data segment code
        type: string
      updatedBy:
        description: Updated By
        type: string
      updatedTimeStamp:
        description: Updated Time Stamp
        type: string
      stageCode:
        description: stageCode
        type: string
        maxLength: 36
      createdBy:
        description: createdBy
        type: string
        maxLength: 36
      status:
        description: status
        type: string
        maxLength: 36
      applicationNumber:
        description: applicationNumber
        type: string  
      remarks:
        description: remarks
        type: string
        maxLength: 300
      processRefNumber:
        description: processRefNumber
        type: string
      trancheParticipantDetails:
        type: array
        items:
          $ref: '#/definitions/TrancheParticipantDetailsModel'
  TrancheParticipantDetailsModel:
    description: TrancheParticipantDetails model
    type: object
    properties:
      id:
        description: Id of the TrancheParticipantDetails
        type: string
      trancheParticipantId:
        description: trancheParticipantId
        type: string
      partyId:
        description: partyId
        type: string
      participantName:
        description: participantName
        type: string
      currency:
        description: currency
        type: string
      assetAmount:
        description: assetAmount
        type: number
      participant:
        description: participant
        type: string
      selfParticipant:
        description: selfParticipant
        type: string
      participantFlag:
        description: participantFlag
        type: string
      assetRatio:
        description: assetRatio
        type: string
  TrancheParticipantCollection:
    description: Collection of TrancheParticipant.
    type: object
    properties:
      TrancheParticipantList:
        type: array
        items:
          $ref: '#/definitions/TrancheParticipantModel'
