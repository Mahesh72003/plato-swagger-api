#----------------------------- OFSS-COPYRIGHT-BEGIN ----------------------------- 
#
#  This source is part of the Oracle Banking Corporate Lending Process Management.
#  Copyright  2018 , Oracle and/or its affiliates.  All rights reserved.
#  
#  No part of this work may be reproduced, stored in a retrieval system, adopted or transmitted in any form or by any means, electronic, mechanical, photographic, graphic, optic recording or otherwise, translated in any language or computer language, without the prior written permission of Oracle and/or its affiliates.
#  
#  Oracle Financial Services Software Limited.
#  Oracle Park, Off Western Express Highway,
#  Goregaon (East), 
#  Mumbai - 400 063, India. 
#  
# ------------------------------- OFSS-COPYRIGHT-END -----------------------------
#
#-----------------------------------------------------------------------------------------------------
#  **
#  ** File Name  : legalremarks.yaml
#  **
#  ** Module     : 
#  ** 

#  -------------------------------------------------------------------------------------------------------
#
#  CHANGE HISTORY
#
#  Created By         :  System
#
#  Bug Number         :  
#  Changed By         :  
#  Change Description :  
#  Change On          :  
#  Search String      :  
#  
#  -------------------------------------------------------------------------------------------------------
swagger: '2.0'
info:
  description: You can find out more about Swagger at [swagger.io](http://swagger.io) and about data types at [Swagger Data Types](https://swagger.io/docs/specification/data-models/data-types/)
  version: 1.0.0
  title: Syndication Application Service
host: 10.184.157.121:9000
basePath: /syndicationapplication-services
tags:
- name: SyndicationApplication
  description: API for SyndicationApplication Operations
schemes:
- http
paths:
  /web/v1/legalremarks:
    post:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - LegalRemarks
      summary: Adds a new LegalRemarks.
      description: Adds a new LegalRemarks. Add additional information here for this endpoint.
      operationId: addLegalRemarks
      parameters:
      - name: LegalRemarks
        in: body
        description: Request Body that contains data required for creating a new LegalRemarks
        required: true
        schema:
          $ref: '#/definitions/LegalRemarksModel'
      responses:
        201:
          description: Returns the Id of the newly created LegalRemarks
          headers:
            Location:
              description: URL of newly created LegalRemarks
              type: string
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - LegalRemarks
      summary: Retrieves the list of existing LegalRemarks.
      description: Retrieves the list of existing LegalRemarks. Add additional information here for the this endpoint.
      operationId: getAllLegalRemarks
      parameters:
      - name: applicationNumber
        in: query
        description: applicationNumber of the user that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Successful performs the operation
          schema:
            $ref: '#/definitions/ResponseModel'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
  /web/v1/legalremarks/{id}:
    get:
      produces:
      - application/json
      - application/xml
      tags:
      - LegalRemarks
      summary: Retrieves the LegalRemarks by id.
      description: Retrieves the LegalRemarks by id. Add additional information here for the this endpoint.
      operationId: getLegalRemarksById
      parameters:
      - name: id
        in: path
        description: Id that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Returns the LegalRemarks based on given applicationNumber.
          schema:
            $ref: '#/definitions/LegalRemarksModel'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
    put:
      consumes:
      - application/json
      - application/xml
      produces:
      - application/json
      - application/xml
      tags:
      - LegalRemarks
      summary: Update an existing LegalRemarks.
      description: Update an existing LegalRemarks. Add additional information here for the this endpoint.
      operationId: updateLegalRemarks
      parameters:
      - name: id
        in: path
        description: id of the LegalRemarks that need to be updated
        required: true
        type: string
      - name: LegalRemarks
        in: body
        description: Request Body that contains data required for updating an existing LegalRemarks
        required: true
        schema:
          $ref: '#/definitions/LegalRemarksModel'
      responses:
        200:
          description: Returns updated LegalRemarks
          schema:
            $ref: '#/definitions/ResponseDtoWrapper'
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        405:
          description: Invalid input
        500:
          description: Internal error
    delete:
      produces:
      - application/json
      - application/xml
      tags:
      - LegalRemarks
      summary: Deletes the LegalRemarks identified by given ID.
      description: Deletes the LegalRemarks identified by given ID. Add additional information here for the this endpoint.
      operationId: deleteLegalRemarks
      parameters:
      - name: id
        in: path
        description: Id of the LegalRemarks that needs to be fetched
        required: true
        type: string
      responses:
        200:
          description: Successful operation.
        400:
          description: Malformed syntax.
        401:
          description: Request does not have sufficient credentials.
        403:
          description: Request is not authorized.
        500:
          description: Internal error
definitions:
  LegalRemarksModel:
    description: LegalRemarks model
    type: object
    properties:
      id:
        description: Id of the LegalRemarks
        type: string
        maxLength: 36
      dataSegmentCode:
        description: Data segment code
        type: string
      updatedBy:
        description: Updated By
        type: string
      updatedTimeStamp:
        description: Updated Time Stamp
        type: string
      stageCode:
        description: stageCode
        type: string
        maxLength: 36
      createdBy:
        description: createdBy
        type: string
        maxLength: 36
      status:
        description: status
        type: string
        maxLength: 36
      applicationNumber:
        description: applicationNumber
        type: string
      processRefNumber:
        description: Process Ref Number 
        type: string  
      remarks:
        description: remarks
        type: string
      legalRemarksDetails:
        type: array
        items:
          $ref: '#/definitions/LegalRemarksDetailsModel'
  LegalRemarksDetailsModel:
    description: LegalRemarksDetails model
    type: object
    required:
    - documentType
    properties:
      id:
        description: Id of the LegalRemarksDetails
        type: string
      legalRemarksId:
        description: legalRemarksId
        type: string
      documentType:
        description: documentType
        type: string
        maxLength: 36
      legalComment:
        description: legalComment
        type: string
      legalRemarksDocument:
        type: array
        items:
          $ref: '../clmo-ds-utility/documentLinkage.yaml#/definitions/DocumentLinkageModel'
  LegalRemarksCollection:
    description: Collection of LegalRemarks.
    type: object
    properties:
      LegalRemarksList:
        type: array
        items:
          $ref: '#/definitions/LegalRemarksModel'
