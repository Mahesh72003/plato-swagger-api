#-----------------------------------------------------------------------------------------------------
#  **
#  ** File Name  : common.yaml
#  **
#  ** Module     : email.notification
#  ** 
#  ** This source is part of the Oracle FLEXCUBE  and is copyrighted by Oracle Financial Services Software Limited.  ** 
#  ** 
#  ** All rights reserved. No part of this work may be reproduced, stored in a retrieval system,
#  ** adopted or transmitted in any form or by any means, electronic, mechanical, photographic,
#  ** graphic, optic recording or otherwise, translated in any language or computer language,
#  ** without the prior written permission of Oracle Financial Services Software Limited.
#  ** 
#  ** Oracle Financial Services Software Limited.
#  ** 10-11, SDF I, SEEPZ, Andheri (East),
#  ** Mumbai - 400 096.
#  ** India
#  ** Copyright 2017-2018 Oracle Financial Services Software Limited. All rights reserved.
#  -------------------------------------------------------------------------------------------------------
#
#  CHANGE HISTORY
#
#  Created By         :  System
#
#  Bug Number         :  
#  Changed By         :  
#  Change Description :  
#  Change On          :  
#  Search String      :  
#  
#  -------------------------------------------------------------------------------------------------------
  

swagger: "2.0"
info:
 description: "This is a sample server eventtemplatemap server." 
 version: "1.0.0"
 title: "eventtemplatemap Service"
host: "api-plato.us.oracle.com:8080"
basePath: "/api/1.0"
consumes:
 - "application/json"
produces:
 - "application/json"
tags:
 - name: "eventtemplatemap"
   description: "cmc email notification is to provide capability to define a template with dynamic values substitute using APIs and send email notification.cmc email notification is to provide capability to define a template with dynamic values substitute using APIs and send email notification."

schemes:
- "https"

paths:

# path to create a new record and fetch all valid records or by intent
#
#  supprorted http methods POST and GET
#
# path example ../eventtemplatemap

 /eventtemplatemap:
  post:
   tags:
    - "eventtemplatemap"
   summary: "Adds a new eventtemplatemap."
   description: "Adds a new eventtemplatemap. Add additional information here for this endpoint."
   operationId: "saveRecord"
   parameters:
    - name: "eventtemplatemap" 
      in: "body" 
      description: "Request Body that contains data required for creating a new eventtemplatemap" 
      required: true
      schema:
       $ref: "#/definitions/CmcTmEventTemplateMappingDTO"
   responses:
    201: 
     description: "Returns message of succes or failure " 
     headers:
      Location:
       description: "URL of newly created eventtemplatemap" 
       type: "string"
     schema:
       $ref: "#/definitions/ResponseDtoWrapper"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    405:
     description: "Invalid input"
    500:
     description: "Internal error"
  get:
   tags:
    - "eventtemplatemap"
   summary: "Get all valid eventtemplatemap."
   description:  "Get all valid eventtemplatemap. This endpoints will be used to Get all valid eventtemplatemap maintenance records."
   operationId: "getAll"
   parameters:
   - name: includecloseandunauth
     in: query
     description: includecloseandunauth need to be set to fetch summary record, defult value is false
     required: false
     type: boolean
   - name: offset
     in: query
     description: offset is starting no of records to be fetched
     required: false
     type: integer
   - name: limit
     in: query
     description: limit is maximum no of records to be fetched
     required: false
     type: integer
   responses:
    201:
     description: "Returns message of succes or failure"
     headers: 
      Location:
       description: ""
       type: "string"
     schema:
       $ref: "#/definitions/CmcTmEventTemplateMappingCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    405:
     description: "Invalid input"
    500:
     description: "Internal error"

# path to modify existing record, delete existing record and fetch valid records by id or by intent( includes latestRecord, version no)
#
#  supprorted http methods PUT, DELETE and GET
#
# path examples----
#   PUT       .../eventtemplatemap/xxx
#   DELETE    .../eventtemplatemap/xxx
#   GET       .../eventtemplatemap/xxx
#   GET       .../eventtemplatemap/xxx?version=latestrecord
#   GET       .../eventtemplatemap/xxx?version=1

 /eventtemplatemap/{id}:
  put:
   tags:
    - "eventtemplatemap"
   summary: "Update an existing eventtemplatemap."
   description:  "Update an existing eventtemplatemap."
   operationId: "modifyRecord"
   parameters:
    - name: "id"
      in: "path"
      description: "key of the eventtemplatemap Maintenance"
      required: true
      type: "string"
    - name: "eventtemplatemap"
      in: "body"
      description: "Request Body that contains data required for updating an existing eventtemplatemap"
      required: true
      schema:
       $ref: "#/definitions/CmcTmEventTemplateMappingDTO"
   responses:
    200:
     description: "Returns updated eventtemplatemap"
     schema:
      $ref: "#/definitions/ResponseDtoWrapper"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    405:
     description: "Invalid input"
    500:
     description: "Internal error"
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrieves the eventtemplatemap by ID."
   description:  "Retrieves the eventtemplatemap by ID."
   operationId: "getById"
   parameters:
    - name: "id"
      in: "path"
      description: "Id of the user that needs to be fetched"
      required: true
      type: "string"
    - name: "version"
      in: "query"
      description: "Id of the user that needs to be fetched"
      required: true
      type: "string"
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
      $ref: "#/definitions/CmcTmEventTemplateMappingWrapperDTO"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"
  delete:
   tags:
    - "eventtemplatemap"
   summary: "Deletes the eventtemplatemap identified by given ID."
   description:  "Deletes the eventtemplatemap identified by given ID. Add additional information here for the this endpoint."
   operationId: "delete"
   parameters:
    - name: "id"
      in: "path"
      description: "Key of the eventtemplatemap maintenance"
      required: true
      type: "string"
    - name: "eventtemplatemap"
      in: "body"
      description: Request body contain eventtemplatemap and Modification no of eventtemplatemap maintenance which need to be deleted"
      required: true
      schema:
       $ref: "#/definitions/ModListKeyDto"
   responses:
    200:
     description: "Successful operation."
     schema:
      $ref: "#/definitions/ResponseDtoWrapper"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

# path to Authorize existing record.
#
#  supprorted http methods PATCH
#
# path examples----
#   PATCH       .../eventtemplatemap/xxx/approve

 /eventtemplatemap/{id}/approve:
  patch:
   tags:
    - "eventtemplatemap"
   summary: "Authorize the eventtemplatemap by given ID."
   description:  "Authorize the eventtemplatemap identified by given ID."
   operationId: "authorize"
   parameters:
    - name: "id"
      in: "path"
      description: "Key of the eventtemplatemap maintenance"
      required: true
      type: "string"
    - name: "eventtemplatemap"
      in: "body"
      description: Request body contain eventtemplatemap and Modification no of eventtemplatemap maintenance which need to be deleted"
      required: true
      schema:
       $ref: "#/definitions/ModListKeyDto"
   responses:
    200:
     description: "Successful operation."
     schema:
      $ref: "#/definitions/ResponseDtoWrapper"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

 /eventtemplatemap/consolidateapprove:
  patch:
   tags:
    - "eventtemplatemap"
   summary: "Authorize the eventtemplatemap by given ID."
   description:  "Authorize the eventtemplatemap identified by given ID."
   operationId: "consolidateapprove"
   parameters:
    - name: "eventtemplatemap"
      in: "body"
      description: Request body contain eventtemplatemap and Modification no of eventtemplatemap maintenance which need to be deleted"
      required: true
      schema:
       $ref: "#/definitions/ModListKeyCollection"
   responses:
    200:
     description: "Successful operation."
     schema:
      $ref: "#/definitions/ResponseDtoWrapperCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"


# path to close existing record
#
#  supprorted http methods PATCH
#
# path examples----
#   PATCH       .../eventtemplatemap/xxx/close
 /eventtemplatemap/{id}/close:
  patch:
   tags:
    - "eventtemplatemap"
   summary: "Close the eventtemplatemap by given ID."
   description:  "Close the eventtemplatemap identified by given ID."
   operationId: "closeRecord"
   parameters:
    - name: "id"
      in: "path"
      description: "Key of the eventtemplatemap maintenance"
      required: true
      type: "string"
    - name: "eventtemplatemap"
      in: "body"
      description: Request body contain eventtemplatemap and Modification no of eventtemplatemap maintenance which need to be closed"
      required: true
      schema:
       $ref: "#/definitions/KeyDto"
   responses:
    200:
     description: "Successful operation."
     schema:
      $ref: "#/definitions/ResponseDtoWrapper"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

# path to reopen existing record
#
#  supprorted http methods PATCH
#
# path examples----
#   PATCH       .../eventtemplatemap/xxx/reopen

 /eventtemplatemap/{id}/reopen:
  patch:
   tags:
    - "eventtemplatemap"
   summary: "reopen the eventtemplatemap by given ID."
   description:  "reopen the eventtemplatemap identified by given ID."
   operationId: "reopenRecord"
   parameters:
    - name: "id"
      in: "path"
      description: "Key of the eventtemplatemap maintenance"
      required: true
      type: "string"
    - name: "eventtemplatemap"
      in: "body"
      description: Request body contain eventtemplatemap and Modification no of eventtemplatemap maintenance which need to be reopen"
      required: true
      schema:
       $ref: "#/definitions/KeyDto"
   responses:
    200:
     description: "Successful operation."
     schema:
      $ref: "#/definitions/ResponseDtoWrapper"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

# path to get all unauthorized records
#
#  supprorted http methods GET
#
# path examples----
#   GET       .../eventtemplatemap/xxx/unauthorized

 /eventtemplatemap/{id}/unauthorized:
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap identified by given ID."
   operationId: "authQuery"
   parameters:
    - name: "id"
      in: "path"
      description: "Id of the user that needs to be fetched"
      required: true
      type: "string"
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
       $ref: "#/definitions/CmcTmEventTemplateMappingCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

# path to  history record for remitterid
#
#  supprorted http methods GETS
#
# path examples----
#   GET       .../eventtemplatemap/xxx/history

 /eventtemplatemap/{id}/history:
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap by ID."
   operationId: "getHistory"
   parameters:
    - name: "id"
      in: "path"
      description: "Id of the user that needs to be fetched"
      required: true
      type: "string"
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
       $ref: "#/definitions/CmcTmEventTemplateMappingCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"
     
 /eventtemplatemap/eventCode/{eventCode}/templateCode/{templateCode}:
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap identified by given ID."
   operationId: "getEventsMapByEventCodeAndTemplateCode"
   parameters:
    - name: "eventCode"
      in: "path"
      description: "eventCode of the event that needs to be fetched"
      required: true
      type: "string"
    - name: "templateCode"
      in: "path"
      description: "templateCode of the event that needs to be fetched"
      required: true
      type: "string"
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
       $ref: "#/definitions/CmcTmEventTemplateMappingCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

 /eventtemplatemap/eventsWithTemplate/eventCode/{eventCode}:
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap identified by given ID."
   operationId: "getEventsWithTemplate"
   parameters:
    - name: "eventCode"
      in: "path"
      description: "eventCode of the event that needs to be fetched"
      required: true
      type: "string"
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
       $ref: "#/definitions/CmcTmEventsWithTemplatesDTO"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

 /eventtemplatemap/alleventsWithTemplate:
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap identified by given ID."
   operationId: "getAllEventsWithTemplate"
   parameters:
   - name: includecloseandunauth
     in: query
     description: includecloseandunauth need to be set to fetch summary record, defult value is false
     required: false
     type: boolean
   - name: offset
     in: query
     description: offset is starting no of records to be fetched
     required: false
     type: integer
   - name: limit
     in: query
     description: limit is maximum no of records to be fetched
     required: false
     type: integer
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
       $ref: "#/definitions/CmcTmEventsWithTemplatesCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"

 /eventtemplatemap/templatesWithEvents/templateCode/{templateCode}:
  get:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap identified by given ID."
   operationId: "getTemplatesWithEvents"
   parameters:
    - name: "templateCode"
      in: "path"
      description: "templateCode of the event that needs to be fetched"
      required: true
      type: "string"
   responses:
    200:
     description: "Returns the eventtemplatemap based on given id."
     schema:
       $ref: "#/definitions/CmcTmTemplatesWithEventsDTO"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    500:
     description: "Internal error"
 /eventtemplatemap/eventsWithTemplate:
  post:
   tags:
    - "eventtemplatemap"
   summary: "Retrives the eventtemplatemap by given ID."
   description:  "Retrives the eventtemplatemap identified by given ID."
   operationId: "postEventsWithTemplate"
   parameters:
    - name: "eventtemplatemap" 
      in: "body" 
      description: "Request Body that contains data required for creating a new eventtemplatemap" 
      required: true
      schema:
       $ref: "#/definitions/CmcTmEventsWithTemplatesDTO"
   responses:
    200:
     description: "Returns updated eventtemplatemap"
     schema:
      $ref: "#/definitions/ResponseDtoWrapperCollection"
    400:
     description: "Malformed syntax."
    401:
     description: "Request does not have sufficient credentials."
    403:
     description: "Request is not authorized."
    405:
     description: "Invalid input"
    500:
     description: "Internal error"
     
definitions:
 CmcTmEventTemplateMappingDTO:
  type: object
  allOf:
  - $ref: '#/definitions/GenericDto'
  - type: object
    properties:
     createdBy:
      type: string
      description: ""
     createdDate:
      type: string
      format: date
      description: ""
     eventCode:
      type: string
      description: ""
     templateCode:
      type: string
      description: ""
     updatedBy:
      type: string
      description: ""
     updatedDate:
      type: string
      format: date
      description: ""
 CmcTmEventsSimpleDTO:
  type: object
  allOf:
  - type: object
    properties:
     id:
      type: string
      description: ""
     eventCode:
      type: string
      description: ""
     eventName:
      type: string
      description: ""
 CmcTmTemplatesSimpleDTO:
  type: object
  allOf:
  - type: object
    properties:
     id:
      type: string
      description: ""
     templateCode:
      type: string
      description: ""
     templateName:
      type: string
      description: ""
     recordType:
      type: string
      description: ""
     modNo:
      type: string
      description: ""
      
 CmcTmEventsWithTemplatesDTO:
  type: object
  allOf:
  - type: object
    properties:
     id:
      type: string
      description: ""
     eventCode:
      type: string
      description: ""
     eventName:
      type: string
      description: ""
     templates:
      type: array
      items: 
       $ref: "#/definitions/CmcTmTemplatesSimpleDTO"
 CmcTmTemplatesWithEventsDTO:
  type: object
  allOf:
  - type: object
    properties:
     id:
      type: string
      description: ""
     templateCode:
      type: string
      description: ""
     templateName:
      type: string
      description: ""
     events:
      type: array
      items: 
       $ref: "#/definitions/CmcTmEventsSimpleDTO"
 CmcTmEventsWithTemplatesCollection: 
  description: "Collection of eventtemplatemapping."
  type: "object"
  properties:
   data:
    type: "array"
    items: 
     $ref: "#/definitions/CmcTmEventsWithTemplatesDTO"
   paging:
    $ref: "#/definitions/PagingDto"
 CmcTmTemplatesWithEventsCollection: 
  description: "Collection of eventtemplatemapping."
  type: "object"
  properties:
   data:
    type: "array"
    items: 
     $ref: "#/definitions/CmcTmTemplatesWithEventsDTO"
   paging:
    $ref: "#/definitions/PagingDto"
 ModListKeyDto:
  type: object
  properties:
   modNos:
    type: array
    items:
     type: integer
   remarks:
    type: string
 ModListKeyCollection: 
  description: "Collection of eventtemplatemapping."
  type: "object"
  properties:
   data:
    type: "array"
    items: 
     $ref: "#/definitions/ModListKeyDto"
 KeyDto:
  type: object
  properties:
   modNo:
    type: integer
   remarks:
    type: string
 ResponseDto:
  type: object
  properties:
   id:
    type: string
   status:
    type: string
   codes:
    type: array
    items:
     $ref: "#/definitions/ResponseCode"
   requestId:
    type: string
 ResponseCode:
  type: object
  properties:
   Code:
    type: string
   Desc:
    type: string
   Type:
    type: string
   Language:
    type: string
   args:
    type: array
    items:
     type: object
   arg:
    type: string
 BindingResult:
  type: object
 GenericDto:
  allOf:
   - type: object
     discriminator: resource_type
 CmcTmEventTemplateMappingCollection: 
  description: "Collection of eventtemplatemapping."
  type: "object"
  properties:
   data:
    type: "array"
    items: 
     $ref: "#/definitions/CmcTmEventTemplateMappingDTO"
   paging:
    $ref: "#/definitions/PagingDto"
 PagingDto:
  type: object
  properties:
   totalResults:
    type: integer
 ResponseDtoWrapper:
  type: object
  properties:
   messages:
    $ref: "#/definitions/ResponseDto"
   data:
    $ref: "#/definitions/ResponseResourceSupport"
 ResponseResourceSupport:
  type: object
 CmcTmEventTemplateMappingWrapperDTO:
  type: object
  properties:
   data:
    $ref: "#/definitions/CmcTmEventTemplateMappingDTO"
 ResponseDtoWrapperCollection: 
  description: "Collection of eventtemplatemapping."
  type: "object"
  properties:
   completeStatus:
    type: string
   data:
    type: "array"
    items: 
     $ref: "#/definitions/ResponseDtoWrapper"
    